# CLAUDE.md

This project's agent-facing spec lives in [`AGENTS.md`](AGENTS.md) - read that first. It covers
the module map, package/class map, build/test commands, the full configuration reference, and the
load-bearing conventions that must not be "simplified" away. Keeping one file as the source of
truth avoids this one drifting out of sync with it.
